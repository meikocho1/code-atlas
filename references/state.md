# State view

Use when the change adds, removes, or restricts meaningful states or transitions. Find the state representation, guards, transition methods, and relevant tests. Label each arrow with the event or operation that causes it. Show a terminal state only if the implementation treats it as terminal.

Keep invalid transitions out of the arrow set and explain important rejections in nearby prose. Do not turn an observed Boolean flag into a full state machine unless the code actually models one.
